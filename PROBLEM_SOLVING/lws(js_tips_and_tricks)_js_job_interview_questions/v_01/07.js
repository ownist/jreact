// log values with variable names smartly
const lib1 = "jQuery";
const lib2 = "React";

console.log(`library - 1: ${lib1}`);
console.log(`library - 2 : ${lib2}`);

// make it smart
console.log({ lib1 });
console.log({ lib2 });
